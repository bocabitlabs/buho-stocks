export function convertDataLinesToList(data: string[][]) {
  console.log("Converting data to list...");
  // Create a list with the data attribute of each object
  const dataRows = data;
  return dataRows;
}
